---
# browserid-ng-0c49
title: Build registry-api-v1 §5.6 account membership (attach/detach/transfer cascade)
status: in-progress
type: feature
priority: normal
created_at: 2026-08-30T18:01:42Z
updated_at: 2026-09-07T21:25:31Z
parent: browserid-ng-9yyk
---

THE RESUME POINTER for the account-membership thread (Dan asked 2026-08-30).

STATUS 2026-09-02: spec r3 WRITTEN and COMMITTED (96eac4a) — self-authenticating attach (possession proofs, no token, no membership record), condensed to 627 lines. Dan is reviewing. The CURRENT design is in the '## Spec-patch rulings' and '## Merged attach design' sections below + docs/specs/registry-api-v1.md itself (§5.6 and §3.2 are the new parts); the paragraph and checklist immediately below PREDATE r3 (r2 era: membership records, devices/register) and the checklist needs rewriting after review. Review aid: field-guide artifact https://claude.ai/code/artifact/fa54e6f5-6c81-4dad-9a46-efa5e3e6f6c4 (§02 flows).

R2-ERA CONTEXT (superseded): Spec was SETTLED — registry-api-v1 §5.6 (synchronous attach/detach with inline browserid-membership-v1 records, transfer-on-proof with loser notification, revoke-and-drop of derived agent children) + §7.1 reasons + §10.8 decision log; design history and parity table live on bean 1sb3; reset-channel mitigations are bean dksx (explore separately).

Implementation checklist (R2-ERA, SUPERSEDED — see '## r5 implementation checklist' at the end of this bean):
- [ ] Registrar: POST /api/v1/account/attach + /api/v1/account/detach; membership-record validation (config-cert bar, grantor ownership, subject match, ≤300s window, jti replay cache)
- [ ] Transfer cascade (shared core): hg2j-scoped cert revocation at the loser + grantor-warrant revocation + derived-agent revoke-and-drop (this also FIXES a93p for the shipped cookie transfer arms — wire them through the same core) + kind:'notice' inbox item + out-of-band notify SHOULD + emptied-account deletion
- [ ] Host/store capabilities: identity ownership moves, agent-children enumeration, notice items in the inbox shape (§5.1)
- [ ] Wallet: bootstrap flow gains 'add to existing account' — when the wallet already holds an anchor, second-identity bootstrap calls attach instead of letting the token exchange mint a parallel account; sign the membership record with the anchor config key
- [ ] Dialog/account page: per Dan's Q6 ruling the account-page email UI invokes the WALLET which talks to the registry — sequence with 71vt (blocked on 71vt's account/consent routing decision landing as implementation)
- [ ] Deployment note dksx-lite: ensure registry-attached rows don't silently join the reset-eligible set (full mitigation design in dksx)
- [ ] Tests: registry_api_test coverage for attach/detach/transfer incl. record replay, last_identity, agent-children cascade, no-existence-leak; SqliteStore test for the membership moves (memory-store rule)

Blocked-by: nothing hard; touches a93p (fixes it). Related: 71vt (surface retirement), dksx (reset hardening), 1sb3 (design record).

## Spec-patch rulings (Dan, 2026-09-01 discussion)

Agreed, pending the r3 spec patch:
1. Membership record DELETED. Instead the §3.2 request proof gains a body-hash claim; the proof (already signed per-call by the config cert the token was minted from) becomes the signed consent artifact for attach/detach. Kills browserid-membership-v1, grantor/subject/subject_config_key, record_* reasons, jti record cache.
2. register + attach take `certs: [<JWS>...]` (each cert self-describes flavor via purpose). No more device_cert/config_cert wire fields; pair-holder check dissolves. Single-flavor issuance (e.g. short-lived auth-only cert for shared computers) is supported policy.
3. POSSESSION REQUIRED (Dan ruling): the certs array carries { cert, proof } pairs — proof = the §3.2 request-proof shape signed by the cert's own key. Self-authenticating both directions (token+proof = account control; per-cert proof = cert control). Kills the RP-replay transfer attack. Freshness (iat ≤ ~300s on transfer-triggering certs) kept as defense in depth (fences a key-thief holding stale loot).
4. Terminology sweep: 'device cert' is the umbrella per core §4.1 (auth cert / config cert are flavors); no 'anchor' as account property — any identity's token can drive membership calls (all emails equal).
5. Condensation pass (~50%) as separate commit after semantic patches.
6. Auth-only devices (shared computer), CORRECTED per Dan 2026-09-01: the common flow is wildcard-holder warrants — trusted device signs+registers warrants matched to the account's browsers.* prefix; an auth-only device presents them with its own access cert (verifier matches holder). Works today via the dialog's cookie lane; MUST keep working. Phone-in-the-loop consent is only the optional fallback for un-warranted sites, not the primary flow.
6b. GAP: the token lane has no warrant-fetch path for auth-only devices (reads need a token, token needs a config cert). r3 adds a warrant-read endpoint authenticated by a possession-signed request from the auth cert (serves warrants whose holder matcher covers the cert's holder — same artifacts RPs already see). Device row recorded at first possession-proven contact (this fetch), not at consent approval. Never-used certs stay issuer-side; cert revocation is issuer-side regardless.
7. Consistency gap found 2026-09-01: /warrant/request (WarrantRequestBody, consent.rs:1271) accepts bare cert bytes with no possession signature — apply ruling 3 there too (fold into r3 or follow-up).

## Merged attach design (Dan + discussion, 2026-09-01, supersedes items 1-3/6 shapes above)

- register + §5.6.1 attach MERGE into one cert-authenticated endpoint, NO token: { account: <routing email>, certs: [{cert, proof}...] }; every proof = §3.2 request-proof shape signed by that cert's key; routing identity must belong to a possession-proven cert in the call.
- TWO-TIER AUTHORITY RULE (closes the stolen-auth-cert escalation: attacker attaches their own config cert to victim account -> mints account-wide token): cert identity already owned by routing account -> RECORDING, auth-cert possession suffices (grant-free; the shared-computer self-attach). Identity not owned (join/transfer) or account creation -> MEMBERSHIP CHANGE, requires config-cert possession from the routing account. purpose:authorization is the boundary marker.
- Account creation: only when array includes a config cert FOR the routing identity; else error (no surprise accounts).
- §3.1 token exchange STOPS auto-creating accounts — errors on never-seen identity ('attach first'). Kills the parallel-account trap structurally.
- detach: same cert-signed auth, config required.
- Transfer cascade, freshness (~300s), no-existence-leak on transfer vs new: unchanged.
- CONFIRMED by Dan 2026-09-02, with one amendment: the 'account' routing field is redundant — the header proof's signer (matched against the array's certs) routes the call; signer's cert must be in the array. r3 SPEC PATCH DRAFTED 2026-09-02 (registry-api-v1.md, uncommitted): §3.1 no-auto-create + no_account reason, §3.2 bh claim + possession-proof shape (shared jti rule), §5.3 register deleted, §5.6 rewritten flows-first (attach/transfer/detach/warrants-fetch/auth-only matrix), §7.1 reasons reworked, §8 invariants 2+7, §9 mapping, §10 decision 9. NOTE: the implementation checklist at the top of this bean predates r3 and needs rewriting after Dan reviews the spec patch (no membership record, no registrar attach-with-record endpoint; certs+proofs arrays instead). Condensation pass DONE 2026-09-02 (same working tree as the r3 semantic patch, uncommitted): 1030→627 lines, prose −42% while ADDING all decision-9 material; §7.1 reason tables now ARE the validation bars (§5.1/§5.6 point at them instead of restating). Field-guide artifact still shows the r2 attach design — update after Dan reviews.

## Review rulings r3 (Dan, 2026-09-02, session 2)

10. Error citations: every refusal in prose is now cited as `<status> <error>/<reason>` (convention stated in §4); §3.1 refusals became a condition→error table; `400 invalid_scope` added to §7. Dan: length-sensitive but clarity wins.
11. v1 warrants: registry §3.1 no longer accepts them (allowance sentence deleted). Core-spec removal tracked as a separate bean (browserid-warrant-v1 compat paragraph, core §5).
12. Confirmed reading: §3.1 'every requested scope' = the warrant's scope claim must cover each entry of the token call's scope field.

13. no_account on attach DELETED as unreachable: the routing cert is always in the array and names the routing identity, so creation with a config routing cert succeeds and with an auth routing cert hits the membership rule's config_required first. no_account keeps its single §3.1 meaning (attach first). Dan's framing kept: same cause in both places, only the remedy differed.

14. §3 gained a lead-in table: token mode (§5.1–5.5) vs cert mode (§5.6), and why the token exists. Dan: unclear what §3.1 tokens were for since attach doesn't use one.

15. CERT AUTH EVERYWHERE (Dan 2026-09-02): token exchange deleted. Every call: Authorization: Cert <JWS> + Proof: <JWS> (§3/§3.1); routing cert's identity names the account; two-tier rule is API-wide (config = full, auth = recording tier: attach-of-self + warrants/fetch, else 403 config_required). attach body = further certs only (MAY be empty); detach body = {identity}; fetch body = {}. §5.5 token_endpoint → endpoint. Errors: invalid_grant/invalid_scope/invalid_token/insufficient_scope gone; 401 invalid_cert (routing) vs 422 invalid_cert (body); no_account is an invalid_cert reason. Decision 10 logged; decisions 5/7 amended. ig9p loses its anchor.

16. Cert travels ONLY on attach (Authorization: Cert); every other call is Proof-only, kid = SHA-256 of the raw pubkey resolved against recorded certs; unrecorded key ⇒ 401 invalid_cert/unknown_key (attach first). Fetch's 'record first-seen cert' clause dropped — attach is the single door, so every acting key is on the device list. no_account reason gone entirely (attach creates; elsewhere unknown_key covers it).

## OPEN QUESTION (Dan, 2026-09-02): IdP power over the account

Any IdP of any identity on the account can mint a config cert and get account-wide authority (revoke/forge consent on other identities' warrants, read the whole site list, attach identities). Not new to r4 (token and cookie lanes had it) but r4 makes it legible. Minimum fix Dan floated: warrant mutations must be routed by a cert matching the warrant's grantor. Full per-identity authority (reads too) means the wallet queries once per config cert or sends several proofs — Dan considers both bad; at that point a token that exchanges a SET of proofs for combined access looks better, but he isn't sure he wants the token back. Also considered: dropping the account entirely (each identity its own principal; kills §5.6/transfer cascade). Legibility lever now in place: every actor is a recorded device (ruling 16); a per-device last-used / audit line would add pressure, not prevention. UNRESOLVED — Dan wants to think more. Do not implement §5.6 authority semantics beyond the current spec until ruled.

## Rulings r5 (Dan, 2026-09-05) — per-identity authority via session token

RESOLVES the IdP-power open question. The account is no longer an authority boundary: it is a membership roster + holder-namespace scope.

17. SESSION TOKEN: `POST /api/v1/session` takes a SET of §3.1 proofs (kids of recorded certs) → opaque token whose authority is the union: roster/namespaces/notices readable by any member; per proven identity, auth tier = read (warrants, requests), config tier = read+write. Token is sender-constrained (my amendment, Dan agreed): every call still carries a Proof signed by a key in the token's set — keeps `bh` consent artifact and no bearer theft. Every call re-checks each member cert's status; any revoked ⇒ token dead.
18. HOLDERS (cross-identity): rename/move/forget need config tier for EVERY identity the holder carries.
19. DETACH = SPLIT, not delete: any config tier may detach any identity (dead-domain escape hatch, Dan); to keep that from being a weapon, detach moves the identity into a fresh account of its own with certs, warrants, agents INTACT — nothing revoked. Destruction stays the issuer's account_cancel. last_identity refusal stays.
20. COOKIE LANE stays account-wide for now → real gap, own bean (child of 71vt, blocked by this), after the token lane ships.
21. ROSTER GUARD (optional, issuer-neutral): account may designate guard identities; a session sees the full roster only if it carries a proof for a guard identity, else only its own identities. Set/clear needs config tier for the guard identity itself. Broker-as-issuer+registry guarding on its own identity = Dan's 'gate the list on the broker cert' idea without privileging the broker in the spec.
22. ATTACH: bare (no token) attach of a cert whose identity is already owned ⇒ recorded (auth cert suffices — the shared-computer flow MUST keep working); not owned ⇒ new account. With token: not owned/owned elsewhere ⇒ joins the token's account, needs config tier on the token (any member); transfer effects unchanged. IdP moving its own identity out / adding its own further identities in is by design.

STATUS 2026-09-05: r5 spec patch DRAFTED in working tree (uncommitted): §3 rewritten around session + subject rule (§3.2/§3.3), §5.6 flows A–E, detach=split, §5.6.7 roster guard, §7 forbidden/invalid_session, §8 invariants, decision 11. Field guide republished as v6. Awaiting Dan's review; commit after. Implementation checklist at top still needs rewriting for r5 (session endpoint, subject table, split, guard).

## Rulings r5, round 2 (Dan, 2026-09-05) — guarded entry; split REJECTED

23. SPLIT REJECTED (Dan): detach-as-split would let a new holder inherit the previous holder's sites/warrants (old broker intent: new owner claims the address, inherits nothing). Detach back to destructive; own write tier, or any write tier + guard.
24. FLOW-B HOLE named: bare attach of a fresh cert for an owned identity let a new owner / the IdP enter the previous holder's account (cookie lane same shape). Ownership epochs on certs REJECTED (lazy/dishonest IdP, taken-over mailbox).
25. GUARD (§5.6.7): registry-defined check delivered as one token — guard page (I1 shape, `browser.guard`) or native `POST /api/v1/guard` kinds (spec registers `password`; wallets fall back to page). Gates: unrecorded key entering an existing account, detach by non-owner, guard changes. Keys entering via guard are BLESSED; blessing transitive through sessions ⇒ roster/devices/holders visible to blessed sessions, no separate gate (Dan confirmed the transitivity reading).
26. Unguarded bare config cert for an owned identity = CHANGE OF OWNERSHIP (transfer + wipe into a new account); wallet must offer the guard first, 'start fresh' explicit. Preserves smooth first use for primary/OIDC users with no password (Dan Q1).
27. Shared computer (Dan): auth-cert-only attach for a preexisting identity needs NO guard, but must not confer blessed status. My narrowing (assumed, flag): unblessed sessions reach warrants/fetch only, and fetch serves only wildcard-holder warrants.
28. Credential reset wording: SHOULD, plain: don't reset via the address being attached. Recovery/reset policy (progressive disclosure w/ threshold, existing-device approval, etc.) is browserid.me policy on dksx, NOT spec.
29. Agents can drive browsers — never assume flows are human-only. Page kept as baseline because it lets registries change strategy without wallet changes; native kinds optional.

STATUS: r5 draft updated in working tree for 23–29; field guide v6 republished. Still uncommitted pending Dan's read of §5.6.2/§5.6.7.

30. (Dan 2026-09-05) Page can't obtain proofs from wallet keys → native guard kind 'identity' restored alongside 'password' (page may approximate by running Y's sign-in itself). 31. Shared computer: warrants/fetch REPLACED by warrants/lookup {audience} — the only unblessed call; MUST rate-limit, SHOULD lock after misses, thresholds registry's. Dan doesn't love the shared-computer status quo; revisit later unless it changes fundamentals.

Review round 3 (Dan 2026-09-06, spec structure): decision log moved to bean 1sb3 (§10 = pointer); §3 intro cut to ~20 lines; conventions now §3, authentication §4 (4.1 proof / 4.2 session / 4.3 subject rule / 4.4 cookie lane); abuse controls folded into §3 + session/attach; discovery moved to §5.1 (inbox 5.2, warrants 5.3, devices 5.4, holders 5.5); 'known gap' sentence and 'issuer-side recording' sentence deleted; proof object defined independent of header/body. Dan to continue from §5.6. Open: whether to pull the §5.6 protocol-shape discussion up out of the endpoints section.

Round 4 (Dan 2026-09-06): tiers made explicit — lookup / read / write (§4.3 table); 'blessed' vocabulary removed everywhere, session response drops the blessed field (roster present at read+); forbidden reasons: not_in_session / read_required / write_required. §5.6 intro rules moved to §4.4 Membership (cookie lane → §4.5); §5.6.1 flows deleted, replaced by an all-flows table in §1 Overview. OPEN (Dan wants explored, NOT additive — as a replacement for §5.2): consent via 'signing intent' — registry renders consent UI, wallet only signs; reuse existing wallet signing APIs (wallet MCP tools authorize/get_assertion; agent lane /warrant/request+poll).

Round 5 (Dan 2026-09-06, readability): endpoint convention set in §5 intro (purpose sentence, request table/line, response, refusals) and applied to §5.2–5.6; attach rewritten as situation×carries→outcome table; transfer effects folded into §4.4 rule 2 ('when an identity leaves an account'); lookup moved under §5.3; auth-only table deleted (tiers in §4.3 cover it); guard restructured as §5.6.3 with kinds as subsections — identity REQUIRED baseline, password OPTIONAL, page OPTIONAL (so 'registry with no guard' case is gone); §4.3 'write on any' and secondary-identity sentences rewritten in plain words. §5.6 now = 5.6.1 attach / 5.6.2 detach / 5.6.3 guard.

Round 6 (Dan 2026-09-06): §4.4 rule 2 shortened; 'when an identity leaves an account' is a separate bulleted paragraph. §5.6.1 attach: checks as bullets; outcomes split into two tables (with a session / without a session), 'change of ownership' language unified as 'leaves B'. GUARD 'identity' KIND DROPPED as an endpoint kind (Dan: too close to session; my finding: a new device holds no recorded keys so it could never have used it). Replaced by the REQUIRED in-attach rule: sign in as a second identity and attach both certs in one bare call; existing devices attach under a session. /guard endpoint keeps only 'password'; page optional.

Round 7 (Dan 2026-09-06): guard fully OPTIONAL — no kind is required; baseline = existing device attaches new certs under its session, bare config cert = new holder. second_identity kind = fresh certs for X and Y in one bare attach (NOT the session endpoint); same-issuer weakness stated, account SHOULD choose which identities count. Kinds: second_identity / password / page.

Round 8 (Dan 2026-09-06): no-guard baseline rejected as a huge UX loss → DEVICE APPROVAL specced on the inbox and REQUIRED of every registry: bare attach w/o guard ⇒ 403 guard_required + kind:'device' inbox request (15 min, keyed on pubkeys, request_code in body); existing device with write tier on the identity approves via requests/respond {code, approve}; new device retries attach until 200. confirm_transfer (start fresh) is now the explicit fallback only. Kinds: device_approval (universal) / additional_identities / password / page. Kind renamed second_identity → additional_identities (Dan).

ADVERSARIAL REVIEW 2026-09-06 (6 fresh reviewers: readability, completeness, coherence, security, implementability, UX). Consolidated page (Dan annotating): see artifact 'registry-api-v1 r5 review'. Top design findings: A1 one mailbox takeover = instant account wipe via confirm_transfer (proposes two-phase hold + quarantine); A2 foreign/ref-less certs never really revoked, session identity set not re-derived (proposes registry-local 'retired' mark); A3 lookup tier = site oracle + device spam per fresh key; A4 attach/session bodies unsigned (bring back header proof on those calls); A5 device approval keyed on pubkey not cert; A6 respond/register accept unrecorded config certs; A7 mixed_accounts precheck makes with-session transfer unreachable; A8 password guard has no auth mode/selector/rate limit. Decisions pending Dan: D1 hold vs immediate takeover; D2 zero-device guard MUST/SHOULD/policy; D3 account/delete endpoint; D4 devices/revoke authority; D5 additional_identities distinct-issuer MUST; D6 sibling-spec drift as beans. ~40 editorial items ready to apply. Impl estimate 4–6 wk server + 1–2 wk wallet, ~60% shipped.

REVIEW ROUND 1 APPLIED 2026-09-06 (Dan's 18 comments on the review page). Rulings: A1 two-phase takeover with Dan's 5 properties → SUSPENSION MODEL in §4.4: leaving (transfer/takeover/detach) suspends the account's rows for the identity (certs fail member check, warrant bits set, agents suspended) for a HOLD (~30 d); last identity ⇒ account quarantined not deleted; identity returning within the hold (under the account's session, or bare past the account's guard) RESTORES; a claimant who can't pass the old account's guard lands in a new account. A3 applied (per-identity rate limits, notice on unguarded attach, self-revoke at lookup tier, discovery `lookup_tier:false` switch — Dan: keep lookup, let broker disable). A4 applied: attach + session carry a header Proof with bh signed by a carried cert's key. A6 applied (config_cert must be recorded/unsuspended; config_cert_not_recorded). A8 applied (guard endpoint: anonymous + Proof by pubkey, identity field, guard_rejected, per-identity lockout, token bound to pubkey+identity, covers all certs in the call). A2/A5/A7 → beans xmqm/giok/ptag (Dan wants separate discussion; A5/A7 fixes applied PROVISIONALLY in the draft, beans say so). D2 SHOULD (registries SHOULD offer a zero-device kind). D3 applied: POST /api/v1/account/delete (§5.6.3; guard → §5.6.4). D4 OPEN: Dan asked for the scenario; kept 'write on every identity the cert names' + any tier for self. D5: additional_identities policy is registry's (MAY), stated in the 403 body as {min, distinct_issuer}. D6 → bean wuoc. Section C applied: §2 definitions (member, validity bar, guard, transfer/takeover), §3 limits + identifier types, §4.1 numbered verification + replay by kid, §4.2 drop-failing-member + cache-ttl re-check + Session-Members header + union token, §4.3 config cell fix + 'on any' defined + reasons, §5.2 grantor default delegator_email + device visibility + claim kinds, §5.3 lookup shape + register indexed/unindexed + forget→unlist + index never reused, §5.4 hides defined + tier/suspended fields, §5.5 assignment proof-authenticated, §5.6.1 atomic + confirm_transfer row + glob refused + request_pending, §5.6.2 exception widened + outage window, §7 typed ids/RFC 6749/reason fields/new reasons, §8 invariants 1–9, §9 fixes, bean/principle refs removed from body. Field guide + review page updated. Spec now 838 lines (grew with delete + hold model; condensation pass may be wanted). Dan: implementability question was really 'can a third party implement from the spec alone' — that is the completeness review; noted.

Review page moved to a clean artifact (old one had stale comment anchors after the status rewrite): 'registry-api-v1 r5 review status'. Dan's two new questions (2026-09-06 15:30–15:40) answered in chat: why lookup_tier is advertised in discovery; why suspended certs keeping the restore-approval power is safe.

HOLD MODEL SIMPLIFIED (Dan 2026-09-06, afternoon): suspension applies to the IDENTITY's data on the account, not to keys — certs stay members, sessions stay alive; ops with a suspended subject refused (identity_suspended); a suspended identity's write tier authorizes attach only. Restore = fresh cert for I attached UNDER the account's session (existing with-session transfer row); bare fresh cert ALWAYS creates a new account, never restores. Restore tier, suspended-cert exception, 'sessions end', bare-restore-past-guard all DELETED. Dan's ping-pong model holds with no exceptions; U2 can never land in A1 (no session there). Next, one at a time: D4, xmqm, giok.

D4 RULED (Dan 2026-09-06): devices/revoke needs write on any ONE identity the cert names (option 1); registries MAY layer extra checks (guard token) on revoking another device's cert. Applied §4.3 + §5.4. Dan feedback: avoid tier jargon ('write on any/every') in discussion — say 'a device holding a config cert for A'; and 'revoke' means the status bit, never 'can't act on this API'.

RESTORE PATH FINAL (Dan 2026-09-06): Dan spotted that a well-behaved IdP revokes U1's old certs on takeover, killing U1's session, so session-based restore fails for the person it's for. Ruling: attach gains an OPTIONAL `recorded` array of possession proofs by certs the registry already recorded (revoked/expired OK, matched by kid, not re-recorded); restore row = fresh config cert for I + such a proof from the account holding I suspended ⇒ I returns there, restored. With-session transfer row also restores (multi-identity case). xmqm CLOSED as part of this: retired mark on revoke/move/forget, cert_retired reason.

giok CLOSED (Dan 2026-09-06): approval = 'this device is me'; cert-hash keying dropped (keyed on pubkey set). Dan, thinking aloud: the edge cases stem from the account model's complexity; consider a more fundamental simplification, e.g. making the account more explicit in the API. No concrete proposal yet — do not act.

EXPLICIT ACCOUNT design note written 2026-09-07 at docs/specs/registry-explicit-account-note.md (uncommitted) + artifact. Thesis: account id (opaque) on the wire in 3 places (session/attach responses; session request; optional attach field). Learned only by being admitted → no oracle; first use unchanged. Attach becomes 'account named?' → door table (3 rows) / membership table (4 rows; membership proven by session OR recorded-key proof — restore is an ordinary row). Removes session/no-session axis, mixed_accounts on session, restore special-casing. Recommendation: do it; edit confined to §4.2, §4.4, §5.6.1. Awaiting Dan's read.

Design note REVISION 2 (Dan 2026-09-07): + one identity at the door (mixed_accounts deleted; additional_identities moves to the guard endpoint) + ACCOUNT DATA (per-identity partition dropped; tiers on the session; subject rule gone; guard is the answer to issuer power; co-members see everything; detach/delete = write; decision 11 rationale to be restated). Resulting §4.3 and §5.6.1 shapes are in the note. Awaiting Dan's read before spec edit.

Note rev 2b (Dan 2026-09-07): 'recorded' (revoked-key) proof REMOVED; restore = attach naming the account id + fresh cert; the id is a SECRET capability (only given to members, wallet stores like a token, never in URLs/logs); registry MAY require its guard on restore. Consequence recorded: leaked id + fresh cert for a suspended identity = restore into the account.

Note rev 2c (Dan 2026-09-07): id is NOT a secret (Dan rejected 2b). Restore = attach naming the account + pass its GUARD (or a session), i.e. an ordinary join; no revoked-key proof, no id-as-capability. Single-device holder with revoked certs and no password cannot restore (= D2 trade).

EXPLICIT ACCOUNT APPLIED to the spec 2026-09-07 (Dan: 'let's apply this'): §4.3 rewritten (tiers on the account; four places an identity is still named); §4.4 leaving/restore rewritten (restore = ordinary join past the guard or under a session); §5.6.1 rewritten (identity + account fields; door table 3 rows; joining table 4 rows); detach/delete = write; additional_identities moved to /guard; session takes {account, proofs} and returns account + tier + roster; not_in_session, mixed_accounts, recorded proofs, subject rule, holder identity rule all deleted. Decision 12 on 1sb3. ptag closed as moot. Field guide + status page updated. Spec 822 lines. NEXT: Dan reads §4.2–4.4 and §5.6.1, then condensation pass?, then COMMIT (spec + note + beans).

Dan 2026-09-07: §4 opener was false ('nothing else authenticates') — the guard IS an auth mechanism. Guard moved to §4.4 (membership → §4.5, cookie lane → §4.6); opener names both authenticators (key possession per call; the guard at first join and whenever the registry asks). NEW rule: a registry MAY re-guard an existing device on any call (403 guard_required); wallet passes the guard and re-opens its session with a `guard` token; token spent by exactly one of session/attach/detach/delete.

Editorial round (Dan 2026-09-07 pm): §4.4 guard rewritten compact (~60 lines: what/when, refusal body, device approval, password, additional identities, page); prior_use DROPPED (the guard_required refusal already says the address is in use); re-guard = one sentence; §4.5 renamed 'Identities and accounts', three plain rules (issuer decides who holds an address / joining needs the guard / leaving puts the identity on hold); §4.6 cookie lane and §9 legacy mapping REMOVED to bean 71vt; §10 removed; §5.4 renamed Certs (/api/v1/certs, certs/revoke, certs/status); §8 trimmed to 6 invariants; all bean references stripped; 'still named' phrasing and 'issuer lands in empty account' fixed. Spec 743 lines.

ADVERSARIAL REVIEW ROUND 2 (2026-09-07, six fresh reviewers on the explicit-account draft: clarity, hostile implementer, coherence, security, UX, design). Consolidated page 'registry-api-v1 review round 2'. A: 9 draft defects (A1 stale retired-key restore sentence + no wire path for restore after issuer revocation [3 reviewers]; A2 takeover collides with auto-filed device request / request_pending; A3 lookup devices can't re-session; A4 §4.5 rules 1–2 vs door table; A5 no bare-key proof mode for guard/page; A6 guard token binding (account/purpose/key) + guard field missing on detach/delete; A7 multi-identity certs skip issuer check; A8 revoked certs vanish from a list with a revoked field; A9 undefined transitions). B: 7 decisions (D1 approval mints a token; D2 re-guard only via session; D3 takeover/restore brake + config cert required; D4 device-request caps + password lockout; D5 deletions: assignment/certs/status/namespaces-vs-core; D6 missing v1 pieces: logout, cross-identity attach rule, status-list timing, orphan drop, lookup notice reason, hold extension; D7 renames: confirm_takeover, owner). C: 6 re-litigations recorded, none recommended to reopen (lookup tier, approval=me, account data, SHOULD zero-device guard, three tiers, hold shape). D: ~50 editorial/typing items. E: sibling drift → wuoc. Raw reports in scratchpad r2-*.md.

ROUND 2 APPLIED 2026-09-07 (Dan's rulings): A1–A9 fixed (retired-key restore sentence deleted; restore = join past the guard; takeover no longer files a request, confirm_takeover supersedes, request_pending gone; lookup devices renew by re-attach, session body without account; §4.5 rules rewritten; guard endpoint carries a CERT+proof (Dan's A5 direction) so §4.1 has no bare-key mode; guard token bound to (account, header-proof key) only, spent by session/attach/detach/delete, refused call doesn't spend; multi-identity issuer check per identity; retired certs stay listed with revoked:true; transitions defined). D1 device approval mints a token via polling POST /guard (202 pending → 200 token). D2 re-guard only via session; request listing/approval never gated. D3 brake on TAKEOVERS only (second takeover within the hold ⇒ guard_required unless old account's guard passed); restores never limited; takeover requires config cert. D4 caps: ≤3 open device requests/account, per-identity rate limit, approval bound to (identity, iss, keys, purposes), password lockout per source never reported. D5: certs/status DROPPED; namespaces aligned to core's three (rename only); holders/assignment KEPT, authenticated by the moved (retired) cert. D6 added: session/end, cross-identity attach = join row, status-list timing, orphan drop, 'joined' notice reason, delete immediate. D7: confirm_takeover; 'owner' for the person. Section D editorial: glossary (issuer, identity, device cert, warrant, holder, recorded/retired, suspended/hold), guard argument once, verification order numbered with statuses, members[] in session body, guarded flag on cert rows, single attach decision table, typed holder fields, tone strips, §8 invariant 3 admits lookup. Spec rewritten whole from §2; awaiting Dan's read; NOT committed.

D3 REVISED (Dan 2026-09-07): the 'second takeover within the hold needs the old account's guard' brake was DROPPED — Dan pointed out the attacker's second move is a restore into their own account past their own guard, so it is a restore fight the registry cannot adjudicate. §4.5 rule 1 now says a contested address flips until the issuer cuts one side off; registry only makes flips visible and reversible. Symmetric per-identity rate limit on account-changing attaches added as a MAY in §3.

2026-09-07 pm: guard_required body unified (guard_kinds = array of {kind,…} objects; page kind carries url; guard_url and browser.guard dropped); §4.4 compacted 92→74 lines (token / refusal / endpoint / kinds as a list); §4.5 compacted 46→41 and de-densified. Spec 785 lines. ADVERSARIAL REVIEW ROUND 3 launched (same six axes, fresh context, reports to scratchpad r3-*.md).

ADVERSARIAL REVIEW ROUND 3 (2026-09-07, six fresh reviewers, reports scratchpad r3-*.md). Consolidated page 'registry-api-v1 review round 3'. A: 12 defects (A1 device-approval poll shape undefined [blocking]; A2 approval shows one cert but attach records two; A3 my rewrite re-introduced 'certs retired on leaving' → hold can't restore; A4 freshness on wrong rows + token clock; A5 page guard needs explicit interaction; A6 revoke+re-register resurrects warrant; A7 attach ambiguities; A8 typing/ordering; A9 connection-request fields vs core; A10 security mediums; A11 foreign-cert 'revoked' UX; A12 sibling drift → wuoc). B: 9 decisions (D1 request code IS the guard token; D2 purpose binding; D3 confirm on transfer row; D4 drop holders/assignment, 409 carries new_holder; D5 re-guard can't deadlock; D6 reorder §4 model→guard→tiers→proof→session, §5.6 after §5.1; D7 grantor/grantee names; D8 sessions list + idempotency + accounts-for-identity; D9 lookup proof-authenticated, no session/row — recommended yes this time). C: 4 re-litigations recorded. D: ~45 editorial.

ROUND 3 APPLIED 2026-09-07 (Dan's rulings). Spec rewritten again with the new ORDER: §4.1 identities/accounts (model) → §4.2 guard → §4.3 tiers → §4.4 proof → §4.5 sessions; §5.1 discovery → §5.2 membership (attach/detach/delete) → §5.3 inbox → §5.4 warrants → §5.5 certs → §5.6 holders. A1+D1: the device-approval request CODE is the token; the retry of the guarded call is the poll (403 guard_required while pending, guard_rejected when denied/expired). A2: guard call carries the full cert set; token bound to (account at mint, cert set); attach must carry exactly that set. A3: 'certs retired on leaving' clause DELETED. A4: freshness on every account-changing case incl. restore; guard row needs none; token clock from mint, window 1 h. A5: page MUST show fingerprints, explicit action, no ambient completion, validate return_origin. A6: revoke ⇒ fresh index next allocation; register never clears a set bit; restore clears only suspension bits. A7/A8: attach as ordered case lists (no account / account named), tier fixed at open, unknown account ⇒ guard_required, held-by = active account, read→write by design stated, retired never revived, identity normalization, records key, bh on every POST, device requests omitted at read, lookup skips suspended, error ordering. A9: request fields per kind (columns); grantor/grantee names (D7); admission reasons not_self_grant/binding_mismatch; deny-first as wallet SHOULD. A10: caps per (identity, source) w/ eviction; guard_kinds registry-wide; status.uri host MUST equal iss; replay entries live to window; recorded identities everywhere; guard SHOULD on delete immediate / other-device revoke / move / forget. A11: wallet MUST revoke at issuer before reporting; joined notice carries cert_id + holder_label (bean 7wj3 noted). D2 WITHDRAWN (no purpose binding — Dan). D3 confirm_takeover on the transfer row; §8 'inherits nothing'. D4 holders/assignment DROPPED; 409 holder_moved carries new_holder. D5 re-guard MUST NOT strand an account. D6 reorder done. D8 GET sessions, session/end {id}, Idempotency-Key header, guard 'account' named by the wallet when it has one. D9 NOT applied (behavior change: no device-list row / no joined notice for shared-computer certs — awaiting Dan). Editorial D applied. Bean-free; no 'row' (records).

D9 DECLINED (Dan 2026-09-07): keep the lookup tier as a recorded row — revoking a shared-computer auth cert from /account matters more than the spec simplification. Recorded as a settled re-litigation; do not raise again unless the revoke-from-account property can be preserved.

COMMITTED 2026-09-07: 45b8f0c 'registry-api-v1 r5: explicit account, guarded entry, account-tier authority' — spec (856 lines), design note, and bean files. Working tree clean. NEXT: rewrite this bean's implementation checklist for r5 (session endpoint, guard kinds, attach case lists, hold/suspend, certs rename, sessions list, idempotency); optional round-4 review against the committed text; then implementation per the implementability plan (cascade → session → tiers → attach → wallet → detach/lookup → guards → shim removal).

## r5 implementation checklist (2026-09-07, supersedes the r2 checklist at the top; spec = registry-api-v1.md @ 45b8f0c)

Order follows the implementability review (riskiest first). Each step lands with a SqliteStore test (memory-store tests miss FK/sentinel bugs) and a registry_api_test.

- [ ] **1. Leaving cascade as one transaction** (§4.1 rule 3): new RegistrarHost method `identity_leaves(account, identity, reason)` — suspend warrants (set bits, mark suspended), suspend derived agents, file `notice`, keep certs/sessions; hold expiry sweeper drops records + accounts with no live cert; restore path clears suspension bits only. Route the cookie lane's transfer arm and `remove_email` through it (fixes a93p).
- [ ] **2. Session-of-proofs** (§4.4–4.5): `POST /session {account, proofs, guard?}` → token bound to member set; `kid` column on cert rows; `Proof` header with `bh` on every POST; per-call member re-check (exp + status via cached lists, no signature re-run); drop failing/retired members; `Session-Members` header; `GET /sessions`, `POST /session/end {id?}`; `Idempotency-Key`. Keep `POST /api/v1/token` as a shim minting the new shape for one release.
- [ ] **3. Tiers** (§4.3): `guarded` flag on cert rows; tier fixed at session open; `read_required` / `write_required` / `identity_suspended` across ~20 handlers; lookup tier reaches only `warrants/lookup` + own `certs/revoke` + `session/end`.
- [ ] **4. Attach** (§5.2.1) replacing `devices/register`: identity + 1–2 certs + possession proofs; holder rule; retired-never-revived; ordered cases (no account / account named); freshness on account-changing cases; `confirm_takeover`; account creation via host; `409 holder_moved` carries `new_holder`.
- [ ] **5. Guard** (§4.2, page-only since 2026-09-08): guard_tokens table (account, cert set, expiry, used); the broker's guard PAGE at `browser.guard` (password check; optional second-identity proof via the login shim, verified with the standard verifier; a way through for a lone device) minting the token server-side bound to the fragment's certs + identity; return over `return_url#guard=` / `#guard_error=`; discovery `guard_kinds: [{kind:"page",url}]`; re-guard on `session` with the no-strand rule. No native kinds, no `/guard` endpoint, no `device` inbox kind.
- [ ] **6. Inbox** (§5.3): `notice` kind; per-kind fields; `grantor`/`grantee` names; admission reasons `not_self_grant`/`binding_mismatch`; claim allocates refs for agent kinds; write tier on claim.
- [ ] **7. Warrants** (§5.4): record key (account, grantor, grantee, audience, scopes); `lookup` (per-identity + per-account rate limit, per-audience miss lock); `register` never clears a set bit, returns `indexing`; revoke ⇒ fresh index on next allocation; `unlist` + `live_warrant`; `suspended` field.
- [ ] **8. Certs** (§5.5) rename from devices: `/api/v1/certs`, `certs/revoke {id|kid}`, retired rows listed with `revoked:true`; wallet routes foreign revoke to the issuer.
- [ ] **9. Holders** (§5.6): three fixed namespaces (rename only); `move` returns `new_holder`, retires old certs; `forget` returns `unrevocable`; drop `holders/assignment`, `certs/status`, namespaces create/delete.
- [ ] **10. Delete** (§5.2.3): `{immediate?, guard?}`; hold then drop; SHOULD guard on immediate.
- [ ] **11. Discovery** (§5.1): `version`, `endpoint`, `status_list`, `browser`, `guard_kinds`, `lookup_tier`; emit alongside the old keys for one release.
- [ ] **12. Wallet** (wallet/src/registry.js, bootstrap.js): session management + re-session on 401; bare attach; guard flow = open the page with the wallet's own loopback/custom-scheme return (bean e98a for the gotchas: mediator in the embedded browser, deferred attach, re-entrancy, identity choice); `warrants/lookup` before RP login; `confirm_takeover` only on an explicit user act; foreign-cert revoke → issuer before reporting.
- [ ] **13. Migration**: cert rows `guarded=1` for existing rows; expose account ids; `DPoP` + `Bearer` accepted one release; delete the token shim, api_tokens table, and old discovery keys afterwards.
- [ ] **14. Tests**: table-driven per-endpoint tier tests; attach case-list tests (all 9 cases); guard kinds; hold/restore incl. contested flip; SqliteStore cascade test; e2e (warm broker on :3000 first).

Blocked-by: nothing hard. Related: zpbh (cookie lane, after step 13), wuoc (sibling drift, after spec is final), 7wj3 (foreign-cert revocation), dksx (broker guard/reset policy), d51o (unlist guard-rails — now `live_warrant`, may close).

## Simplification round (Dan 2026-09-07, evening) — committed 27731c4

Dan: "I resisted it but I relent." Rulings + edits:
- (1) LOOKUP TIER DROPPED, and tier vocabulary with it. Shared computer = a new device (device approval or password). §4.3 is now one rule: calls that change or sign for the account need a **config-cert member** (403 forbidden/config_cert_required); §5 marks them **Config**. read_required/write_required/identity_suspended/lookup_tier/guarded flag/Session-Members/'joined' notice all gone. Session body: no tier; members carry purpose.
- (2) GUARD: OPEN. Dan asked whether the page could run issuer sign-in for additional identities; my answer: mechanically yes, but that normalizes the exact phishing shape (any page can already open the issuer ceremony with its own keys; only issuer UX defends). Proposal on the table: page = required kind (covers password + device approval), additional_identities DEFERRED from v1, registries MAY add kinds. §4.2 kinds untouched pending ruling.
- (3) create/attach split DROPPED (2nd device doesn't know the account id).
- (4) Re-guard = session invalidation: registry ends the session, next `session` may answer guard_required; SHOULD NOT strand an account. Applied §4.2 + §4.5.
- (5) HOLD kept (undo of warrant loss), decoration stripped: identity_suspended reason, 'held at the door' sentence, `suspended` warrant field (listed revoked:true), quarantine-vs-delete unified, delete `immediate` dropped, notice reasons → left/returned, hold_until dropped.
- (6) UNINDEXED WARRANTS GONE: register requires the ref allocate_status holds for the record key (status_ref_mismatch); unlist/confirm_unrevoked/live_warrant/no_status_ref/indexing deleted; registry MAY drop expired records. holders/move deleted (+holder_moved, moving_to, already_in_namespace). GET sessions + end-other deleted (holders/forget = 'remove this device'; retiring a cert ends sessions). Idempotency-Key deleted.
Spec 856 → 800 lines. NEXT: Dan rules on (2); then field guide + status artifact refresh; commit.

Filed 2026-09-07: i63t (core wallet-to-IdP authentication gap found via item 2 — guard kinds stay as-is until it lands) and fp66 (§5.4 warrant-registry mechanics review). Both blocked-by this bean.

Update 2026-09-07: i63t shelved (deferred). The live attack was closed by qze7 (accepted return origins, deployed). Guard kinds are no longer waiting on i63t; the page-only guard simplification in item 2 can proceed on its own merits.


## Guard ruling (Dan 2026-09-08) — item (2) CLOSED, applied to spec

`page` is the ONE required guard kind; `device_approval`, `password`, `additional_identities` and `POST /api/v1/guard` are gone from v1 (registries MAY add kinds; wallets ignore unknown ones). The `device` inbox kind and its `respond` special case are gone. `guard_rejected` now means only a bad token. The page may obtain proof of another identity through the login mediator like any site — no protocol change; wallets expose the mediator inside their embedded browser. Wallet gotchas (deferred attach, re-entrancy, identity choice) tracked on bean e98a. Dan has guard-page-only ideas for helping the user choose an identity. Spec 800 → 764 lines. NEXT: field guide + review-status artifact refresh, then start checklist step 1.
