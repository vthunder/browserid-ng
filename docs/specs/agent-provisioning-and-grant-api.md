# Agent Provisioning, Warrants & Grant Exchange — folded into the core spec

> **This document has been merged into the [protocol specification](./browserid-ng-protocol.md).**
> An agent is just a **holder**, so its provisioning and use are the core protocol,
> not a separate module. What used to live here is now:
>
> - **Headless issuance (device-grant)** → core **§7.4**
> - **Access-cert minting** → core **§7.2**
> - **Warrants & the presentation bundle** → core **§4.3, §5**
> - **Just-in-time warrant consent flow** → core **§7.5**
> - **Grant exchange (RFC 7521 bundle → token)** → core **§9** (optional)
> - **Verification** → core **§6** (identical for every holder)
>
> This stub is kept only so existing links resolve; it will be removed once
> references are updated.
